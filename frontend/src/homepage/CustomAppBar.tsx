import { AppBar, TextField } from '@mui/material';

export default function CustomAppBar() {
    return(
        <AppBar>
            <TextField>
                Search
            </TextField>
        </AppBar>
    );
}